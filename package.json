{
  "name": "cycle",
  "version": "1.0.0",
  "description": "",
  "main": " ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cycle/core": "1.0.x",
    "@cycle/web": "2.2.2",
    "babel-core": "^6.0.17",
    "body-parser": "^1.14.1",
    "express": "^4.13.3",
    "rx": "^4.0.6"
  },
  "devDependencies": {
    "browserify": "11.0.1",
    "babel": "5.6.x",
    "babelify": "6.1.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browserify": "browserify raw.js -t babelify --outfile compiled.js",
    "start": "npm install && npm run browserify && node server.js"
  }
}
